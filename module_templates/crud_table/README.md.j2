# {{ name }} Module

{{ description }}

## Features

- ✅ CRUD operations (Create, Read, Update, Delete)
- ✅ Pagination support
- ✅ Filtering and sorting
- ✅ Multi-tenant isolation
- ✅ Role-based access control

## API Endpoints

- `GET /api/v1/{{ module_code.replace("_", "-") }}/` - List items (paginated)
- `POST /api/v1/{{ module_code.replace("_", "-") }}/` - Create item
- `GET /api/v1/{{ module_code.replace("_", "-") }}/{id}` - Get item by ID
- `PATCH /api/v1/{{ module_code.replace("_", "-") }}/{id}` - Update item
- `DELETE /api/v1/{{ module_code.replace("_", "-") }}/{id}` - Delete item

## Data Fields

{% for field in fields %}
- **{{ field.name }}** ({{ field.type }}): {{ field.label }}{% if field.required %} [필수]{% endif %}

{% endfor %}

## Installation

This module is generated by TriFlow Module Generator.

1. Restart backend: `uvicorn app.main:app --reload`
2. Restart frontend: `npm run dev`
3. Enable in Settings → Tenant Modules

## Development

To customize this module, edit:
- Backend logic: `backend/service.py`
- API endpoints: `backend/router.py`
- UI components: `frontend/{{ pascal_name }}Page.tsx`

## Version

- **Version**: 1.0.0
- **Generated**: {{ generation_date }}
- **Generator**: TriFlow Module Generator
