/**
 * Shared Hooks for TriFlow Modules
 *
 * These hooks eliminate repetitive code and provide consistent
 * data fetching, pagination, and filtering across all modules.
 */
export { useModuleData } from './useModuleData';
export { useModuleTable } from './useModuleTable';
export { useModuleFilters } from './useModuleFilters';
